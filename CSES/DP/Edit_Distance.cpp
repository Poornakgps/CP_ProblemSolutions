#include<bits/stdc++.h>

using namespace std;
#define ll long long
#define vi vector<ll>
#define mod 1000000007

void solve(){
    string a, b;
    cin>>a>>b;
    int n = a.size(), m = b.size();
    vector<vector<ll>> dp(n+1, vector<ll>(m+2, 0));
    
}

signed main(){

    int t=1;
    // cin>>t;
    while(t--){
        solve();
    }
}